namespace SupportTicketApi.Models;

/// <summary>Declared least to most severe. The numeric values are the severity order.</summary>
public enum TicketPriority
{
    Low = 0,
    Normal = 1,
    High = 2,
    Urgent = 3
}
